
---

## Abhi Tak Kya Hua

4.4.5 mein naam aur email ke validation rules add kiye.

Ab `z.infer` se TypeScript type automatically banayenge — aur form final karenge.

---

## `z.infer` — Schema Se TypeScript Type Banao

Abhi tak hum manually `FormFields` type bana rahe the:

```tsx
type FormFields = {
  naam: string
  email: string
}
```

Par yeh **duplicate** hai — schema mein bhi wahi define kiya hai. Agar schema change hua — type manually update karna padega.

**Doubly maintenance** — ek jagah change kiya, doosri jagah bhool gaye — bug aa gaya.

## Solution: `z.infer`

Zod ka `z.infer` schema se automatically TypeScript type nikal deta hai:

```tsx
const formSchema = z.object({
  naam: z.string().min(1, 'Naam zaroori hai'),
  email: z.email('Email sahi format mein nahi hai'),
})

// Schema se type automatically nikalo
type FormFields = z.infer<typeof formSchema>
```

**`z.infer<typeof formSchema>`** — iska matlab "formSchema ki type nikalo."

Zod khud samajh jata hai:
- `z.string()` → `string`
- `z.email()` → `string`
- `.min(1)` → still `string` (validation rule, type nahi badalta)

Toh `FormFields` automatically ban jayega:

```ts
type FormFields = {
  naam: string
  email: string
}
```

Ab `useForm` mein yeh type use karo:

```tsx
const { register, handleSubmit, formState: { errors } } = useForm<FormFields>({
  resolver: zodResolver(formSchema)
})
```

Ab agar schema mein kuch change kiya — type automatically update ho jayega. No more doubly maintenance.

---

## Poora `ContactForm.tsx` — Final Version

```tsx

import { useForm } from 'react-hook-form'
import { zodResolver } from '@hookform/resolvers/zod'
import { z } from 'zod'

// Step 1: Schema banao
const formSchema = z.object({
  naam: z.string().min(1, 'Naam zaroori hai'),
  email: z.email('Email sahi format mein nahi hai'),
})

// Step 2: Schema se type nikalo
type FormFields = z.infer<typeof formSchema>

export default function ContactForm() {
  // Step 3: useForm mein resolver aur type do
  const { register, handleSubmit, formState: { errors } } = useForm<FormFields>({
    resolver: zodResolver(formSchema)
  })

  return (
    <form
      onSubmit={handleSubmit((data) => {
        console.log(data)
      })}
      className="flex flex-col gap-4 max-w-md"
    >
      {/* Naam */}
      <div>
        <label className="block text-sm font-medium mb-1">Naam</label>
        <input
          type="text"
          placeholder="Apna naam likho"
          className="border border-gray-300 rounded px-3 py-2 w-full"
          {...register('naam')}
        />
        {errors.naam && (
          <p className="text-red-500 text-sm mt-1">{errors.naam.message}</p>
        )}
      </div>

      {/* Email */}
      <div>
        <label className="block text-sm font-medium mb-1">Email</label>
        <input
          type="email"
          placeholder="Apni email likho"
          className="border border-gray-300 rounded px-3 py-2 w-full"
          {...register('email')}
        />
        {errors.email && (
          <p className="text-red-500 text-sm mt-1">{errors.email.message}</p>
        )}
      </div>

      <button
        type="submit"
        className="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600"
      >
        Submit
      </button>
    </form>
  )
}
```

---

## Test Karo — Saari Validations

1. **Sab khali chhod ke submit karo** — 2 errors dikhengi (naam, email)
2. **Sirf naam bharo** — email ka error dikhega
3. **Naam + sahi email bharo** — submit hoga, console mein data dikhega ✅

---

## Summary

✅ **`z.infer<typeof schema>`** — schema se TypeScript type automatically nikalta hai  
✅ No more doubly maintenance — schema change hua toh type automatically update  
✅ **Complete flow**: Schema → `z.infer` → Type → `useForm<Type>` → `zodResolver(schema)`  
✅ Saari validations kaam kar rahi hain — 2 fields, 2 error messages  

---

## Journey Complete 🏆

```
4.3    → Manual form + validation (problem dikhi)
4.4.1  → RHF basics (register, handleSubmit)
4.4.3  → RHF built-in validation (required, pattern)
4.4.4  → Zod intro (schema, zodResolver)
4.4.5  → Zod rules (min, email, refine)
4.4.6  → RHF + Zod complete form (z.infer)
```
