# 5.5 useReducer — Complex State Management

---

## Abhi Tak Kya Hua

5.1 se 5.4 tak performance optimization seekhi — memo, useMemo, useCallback. Mini-project bhi banaya.

Ab baat karte hain **complex state** ki. Kabhi kabhi `useState` kaafi nahi hota — jab state ka structure complex ho, ya multiple state changes ek saath handle karne hoon.

---

## Problem — Jab useState Mushkil Ho Jaata Hai

Socho ek shopping cart:

```tsx
const [items, setItems] = useState<CartItem[]>([])
const [isOpen, setIsOpen] = useState(false)
const [coupon, setCoupon] = useState('')

function addItem(product: Product) {
  setItems(prev => [...prev, { ...product, quantity: 1 }])
}

function updateQuantity(id: number, qty: number) {
  setItems(prev => prev.map(item =>
    item.id === id ? { ...item, quantity: qty } : item
  ))
}

function applyCoupon(code: string) {
  // validation
  // update discount
  // maybe clear some items?
  // teen cheezein ek saath change karni hain
}
```

Dekh rahe ho? `items`, `isOpen`, `coupon` — teen alag `useState` calls. Lekin kuch actions ek saath multiple states change karte hain. Toh state scattered ho jati hai, aur debugging mushkil ho jati hai.

**Yeh problem kyun hai?** `useState` independent variables hai. React ko pata nahi ki yeh teeno ek saath belong karte hain. Koi action multiple states change kare toh logic scattered ho jata hai — ek jagah `setItems`, doosri jagah `setCoupon`. Bugs aana easy hai.

---

## useReducer — Kya Hai

`useReducer` ek hook hai jo complex state logic ko ek central jagah manage karta hai. Teen cheezein hoti hain:

1. **State** — poora state object ek saath
2. **Action** — ek object jo batata hai "kya karna hai"
3. **Reducer** — ek function jo action ke according state change karta hai

Syntax:

```tsx
const [state, dispatch] = useReducer(reducer, initialState)

// state — current state
// dispatch(action) — action bhejne ka tarika
// reducer — function jo decide karta hai state kaise change hogi
```

**Kya hota hai internally?** Jab `dispatch(action)` call karte ho, React internally `reducer(currentState, action)` call karta hai. Jo bhi reducer return karta hai — woh nayi state ban jati hai. Simple.

Visual mapping:

```
View (UI)
  │ dispatch({ type: 'increment' })
  ▼
reducer(state, action)
  │ switch(action.type)
  ▼
New state
  │
  ▼
Component re-renders
```

---

## Minimal Code — Counter with useReducer

Pehle simplest example — counter:

```tsx
import { useReducer } from 'react'

type CounterAction = { type: 'increment' } | { type: 'decrement' } | { type: 'reset' }

function counterReducer(state: number, action: CounterAction): number {
  switch (action.type) {
    case 'increment':
      return state + 1
    case 'decrement':
      return state - 1
    case 'reset':
      return 0
    default:
      return state
  }
}

export default function Counter() {
  const [count, dispatch] = useReducer(counterReducer, 0)

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => dispatch({ type: 'increment' })}>+</button>
      <button onClick={() => dispatch({ type: 'decrement' })}>-</button>
      <button onClick={() => dispatch({ type: 'reset' })}>Reset</button>
    </div>
  )
}
```

Dekho — teen alag `useState` nahi lage. Ek reducer hai jo saare actions handle karta hai. `dispatch({ type: 'increment' })` — action bheja, reducer ne handle kiya, state update hui.

---

## TypeScript Ke Saath — Discriminated Unions

TypeScript mein action types ko properly type karte hain. Discriminated union ka matlab — union type jisme ek common property (`type`) hoti hai jisse TypeScript automatically discriminate (identify) kar sakta hai ki kaunsa action hai.

```tsx
type CartAction =
  | { type: 'ADD_ITEM'; product: Product }
  | { type: 'REMOVE_ITEM'; id: number }
  | { type: 'UPDATE_QTY'; id: number; quantity: number }
  | { type: 'APPLY_COUPON'; code: string }
```

Fayda — TypeScript automatically samajh leta hai ki `ADD_ITEM` mein `product` hai, `REMOVE_ITEM` mein `id` hai. Har action ke andar switch case mein proper auto-complete milega.

---

## Real Example — Shopping Cart with useReducer

```tsx
import { useReducer } from 'react'

interface CartItem {
  id: number
  name: string
  price: number
  quantity: number
}

type CartAction =
  | { type: 'ADD_ITEM'; product: Omit<CartItem, 'quantity'> }
  | { type: 'REMOVE_ITEM'; id: number }
  | { type: 'UPDATE_QTY'; id: number; quantity: number }
  | { type: 'APPLY_COUPON'; code: string }
  | { type: 'CLEAR_CART' }

interface CartState {
  items: CartItem[]
  coupon: string
  discount: number
}

const initialCart: CartState = {
  items: [],
  coupon: '',
  discount: 0,
}

function cartReducer(state: CartState, action: CartAction): CartState {
  switch (action.type) {
    case 'ADD_ITEM': {
      const existing = state.items.find(i => i.id === action.product.id)
      if (existing) {
        return {
          ...state,
          items: state.items.map(i =>
            i.id === action.product.id
              ? { ...i, quantity: i.quantity + 1 }
              : i
          ),
        }
      }
      return {
        ...state,
        items: [...state.items, { ...action.product, quantity: 1 }],
      }
    }

    case 'REMOVE_ITEM':
      return {
        ...state,
        items: state.items.filter(i => i.id !== action.id),
      }

    case 'UPDATE_QTY':
      return {
        ...state,
        items: state.items.map(i =>
          i.id === action.id ? { ...i, quantity: action.quantity } : i
        ),
      }

    case 'APPLY_COUPON':
      // Coupon logic
      return {
        ...state,
        coupon: action.code,
        discount: action.code === 'SAVE10' ? 10 : 0,
      }

    case 'CLEAR_CART':
      return initialCart

    default:
      return state
  }
}
```

Ab component mein use karo:

```tsx
export default function Cart() {
  const [cart, dispatch] = useReducer(cartReducer, initialCart)

  return (
    <div>
      {cart.items.map(item => (
        <div key={item.id}>
          <p>{item.name} x {item.quantity} = ${item.price * item.quantity}</p>
          <button onClick={() => dispatch({ type: 'REMOVE_ITEM', id: item.id })}>
            Remove
          </button>
        </div>
      ))}
      <button onClick={() => dispatch({ type: 'CLEAR_CART' })}>
        Clear Cart
      </button>
    </div>
  )
}
```

Notice kiya? Har action type ke hisaab se properly typed payload aata hai. `REMOVE_ITEM` mein sirf `id` chahiye — TypeScript enforce karega. `APPLY_COUPON` mein `code` chahiye. Galti se extra property bhej di toh TypeScript error dega.

---

## Flow — Jab User "Add to Cart" Click Karta Hai

1. User click karta hai → `dispatch({ type: 'ADD_ITEM', product })`
2. React internally `cartReducer(currentState, action)` call karta hai
3. Reducer switch case 'ADD_ITEM' enter karta hai
4. Check karta hai — product already exists? Ha → quantity increment. Nahi → new item add
5. Naya state object return karta hai
6. React state update karta hai, component re-render hota hai
7. UI mein naya item dikhta hai

---

## useReducer vs useState — Kab Kya Use Karein

**useState use karo jab:**
- State simple ho (number, string, boolean)
- Ek ya do independent state variables ho
- State logic simple ho — ek action ek variable change kare

**useReducer use karo jab:**
- State object complex ho (multiple fields)
- Multiple actions same state ko change karte hoon
- Actions ke beech dependencies hoon (ek action multiple fields change kare)
- State logic ko centralize karna ho (debugging easy)

---

## Common Mistake — Reducer Mein Side Effects

Reducer **pure function** hona chahiye. Andar API call mat karo, localStorage mat likho, `Math.random()` mat karo:

```tsx
// ❌ Galat — side effect reducer mein
function reducer(state, action) {
  case 'SAVE':
    localStorage.setItem('cart', JSON.stringify(state)) // ❌ side effect!
    return { ...state, saved: true }
}

// ✅ Sahi — side effect component mein, reducer sirf state change kare
function reducer(state, action) {
  case 'SAVE':
    return { ...state, saved: true }
}

// Component mein:
useEffect(() => {
  localStorage.setItem('cart', JSON.stringify(cart))
}, [cart])
```

---

## Summary

✅ **useReducer** — complex state ke liye — ek central function saare state changes handle karta hai  
✅ **Action** — object jisme `type` hota hai aur optional payload  
✅ **dispatch(action)** — action bhejne ka tarika  
✅ **Reducer** — pure function — `(state, action) => newState`  
✅ **TypeScript + discriminated unions** — har action type ke hisaab se proper typing  
✅ useState simple ke liye, useReducer complex ke liye  
✅ Reducer pure rakhna — side effects bahar karo

---

## Agla Step

State management ka ek powerful tool seekh liya. Ab baat karte hain **bundle size** ki — jab app bada ho jata hai, toh initial load time kaise kam karein. React.lazy, Suspense, Code Splitting — yeh sab 5.6 mein.
