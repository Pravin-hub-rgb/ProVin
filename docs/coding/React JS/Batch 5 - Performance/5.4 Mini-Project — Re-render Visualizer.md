# 5.4 Mini-Project — Re-render Visualizer

---

## Abhi Tak Kya Hua

5.1, 5.2, 5.3 mein teen hooks seekhe — `React.memo`, `useMemo`, `useCallback`. Ab ek chhota project banate hain jo teeno ko ek saath use kare.

Idea simple hai — ek app jisme har component ka render count dikhe, taahi Profiler ke saath fark clearly dikhe.

---

## Project Setup

```
bun create vite@latest re-render-visualizer --template react-ts
cd re-render-visualizer
bun install
bun run dev
```

Tailwind optional hai — styling ke liuse basic CSS chalega.

---

## Step 1 — Visualizer Component

Pehle ek helper component banate hain jo render count track kare. Yeh har component ke andar use hoga:

```tsx
// RenderCount.tsx
import { useRef, useEffect } from 'react'

export default function RenderCount({ label }: { label: string }) {
  const count = useRef(0)
  count.current++

  return (
    <div className="text-xs text-gray-400 mb-1">
      {label} rendered: {count.current} times
    </div>
  )
}
```

`useRef` use kiya hai kyunki hume render count store karna hai bina re-render trigger kiye. Har render pe `count.current++` hota hai, lekin React ko pata nahi chalta — isliye infinite loop nahi hota.

---

## Step 2 — Problem Dikhane Wala Version

Pehle woh version banate hain jisme koi optimization nahi — taaki problem clearly dikhe:

```tsx
// App.tsx
import { useState } from 'react'
import RenderCount from './RenderCount'

function ExpensiveCard({ value }: { value: number }) {
  return (
    <div className="border p-4 rounded">
      <RenderCount label="ExpensiveCard" />
      <p>Value: {value}</p>
    </div>
  )
}

function Sidebar() {
  return (
    <div className="border p-4 rounded bg-gray-50">
      <RenderCount label="Sidebar" />
      <p>Sidebar — kuch nahi badalta</p>
    </div>
  )
}

export default function App() {
  const [count, setCount] = useState(0)
  const [value, setValue] = useState(42)

  return (
    <div className="p-8 space-y-4">
      <RenderCount label="App" />
      
      <div className="space-x-2">
        <button onClick={() => setCount(c => c + 1)}>
          Re-render App ({count})
        </button>
        <button onClick={() => setValue(v => v + 1)}>
          Change Value ({value})
        </button>
      </div>

      <div className="flex gap-4">
        <ExpensiveCard value={value} />
        <Sidebar />
      </div>
    </div>
  )
}
```

Ab browser mein dekh. Har baar koi bhi button click karo — sab components re-render ho rahe hain:
- `App` — obviously, state change hui
- `ExpensiveCard` — value change nahi hui, phir bhi render ho raha
- `Sidebar` — kuch change nahi hua, phir bhi render ho raha

---

## Step 3 — React.memo Lagao

Pehle `Sidebar` ko memo karte hain — iske koi props nahi, kabhi change nahi hona chahiye:

```tsx
import { memo } from 'react'

const Sidebar = memo(function Sidebar() {
  return (
    <div className="border p-4 rounded bg-gray-50">
      <RenderCount label="Sidebar" />
      <p>Sidebar — kuch nahi badalta</p>
    </div>
  )
})
```

Ab `ExpensiveCard` ko bhi memo karo:

```tsx
interface ExpensiveCardProps {
  value: number
}

const ExpensiveCard = memo(function ExpensiveCard({ value }: ExpensiveCardProps) {
  return (
    <div className="border p-4 rounded">
      <RenderCount label="ExpensiveCard" />
      <p>Value: {value}</p>
    </div>
  )
})
```

Ab test karo:

- Pehli baar render — sab 1 baar
- "Re-render App" click — sirf `App` render hoga. `ExpensiveCard` aur `Sidebar` skip. Dekha? `value` change nahi hui, isliye memo ne render skip kiya.
- "Change Value" click — `App` re-render, `ExpensiveCard` re-render (kyunki `value` change hui), `Sidebar` skip.

---

## Step 4 — Problem — Inline Function Se Memo Toot Gaya

Ab ek `onClick` prop add karte hain `ExpensiveCard` mein — jaise real app mein hota hai:

```tsx
function App() {
  // ...

  const handleClick = () => {
    console.log('card clicked')
  }

  return (
    // ...
    <ExpensiveCard value={value} onClick={handleClick} />
    // ...
  )
}
```

Ab "Re-render App" click karo — `ExpensiveCard` phir se render ho raha hai! `Sidebar` theek hai lekin `ExpensiveCard` memo fail ho gaya.

**Kyun?** Har render pe `handleClick` naya function ban raha hai. Naya reference = naya prop = memo useless.

---

## Step 5 — useCallback Se Fix

```tsx
import { useCallback } from 'react'

const handleClick = useCallback(() => {
  console.log('card clicked')
}, []) // koi dependency nahi — kabhi change nahi hoga
```

Ab test karo — `ExpensiveCard` phir se skip kar raha hai. Function reference stable ho gaya.

---

## Step 6 — useMemo — Derived Data Cache Karna

Ab ek heavy calculation add karte hain — socho ek function hai jo bahut time leta hai:

```tsx
function slowCalculation(num: number): number {
  console.log('⚡ slow calculation chal raha hai')
  let result = 0
  for (let i = 0; i < 10000000; i++) {
    result += num * i
  }
  return result
}

function App() {
  const [count, setCount] = useState(0)
  const [value, setValue] = useState(42)

  const computed = slowCalculation(value) // ❌ Har render pe chal raha hai

  return (
    // ...
    <p>Computed: {computed}</p>
    // ...
  )
}
```

Har baar "Re-render App" click pe `slowCalculation` chal raha hai — chahe `value` change nahi hui. Browser freeze ho raha hai.

UseMemo se fix:

```tsx
const computed = useMemo(() => slowCalculation(value), [value])
```

Ab `slowCalculation` sirf tab chalega jab `value` change ho. Count change pe — cached result use hoga.

---

## Step 7 — Final Component Tree with All Optimizations

```tsx
import { useState, useMemo, useCallback, memo } from 'react'

const Sidebar = memo(function Sidebar() {
  return (
    <div className="border p-4 rounded bg-gray-50">
      <RenderCount label="Sidebar" />
      <p>Sidebar — static content</p>
    </div>
  )
})

const ExpensiveCard = memo(function ExpensiveCard({
  value, onClick
}: {
  value: number
  onClick: () => void
}) {
  return (
    <div className="border p-4 rounded" onClick={onClick}>
      <RenderCount label="ExpensiveCard" />
      <p>Value: {value}</p>
    </div>
  )
})

export default function App() {
  const [count, setCount] = useState(0)
  const [value, setValue] = useState(42)

  const handleClick = useCallback(() => {
    console.log('card clicked')
  }, [])

  const computed = useMemo(() => {
    console.log('⚡ heavy calculation')
    let result = 0
    for (let i = 0; i < 10000000; i++) result += value * i
    return result
  }, [value])

  return (
    <div className="p-8 space-y-4">
      <button onClick={() => setCount(c => c + 1)}>
        Re-render App ({count})
      </button>
      <button onClick={() => setValue(v => v + 1)}>
        Change Value ({value})
      </button>
      <p>Computed: {computed}</p>
      <ExpensiveCard value={value} onClick={handleClick} />
      <Sidebar />
    </div>
  )
}
```

Render counts dekhna:

| Action | App | ExpensiveCard | Sidebar | slowCalculation |
|--------|-----|---------------|---------|-----------------|
| Initial | 1 | 1 | 1 | चला |
| Re-render App | 2 | 1 (skip) | 1 (skip) | nahi chala |
| Change Value | 3 | 2 | 1 (skip) | chala (value change) |
| Re-render App | 4 | 2 (skip) | 1 (skip) | nahi chala |

---

## React Profiler Se Verify Karo

1. Chrome DevTools kholo → Components tab → Profiler (⚡ icon)
2. "Record" button daba ke interactions karo (button clicks)
3. Flamegraph dekho — jahan bar nahi hai (grey), wahan render nahi hua

Maine pehli baar Profiler use kiya tha toh mujhe samajh nahi aaya tha ki grey bars ka matlab kya hai. Grey = component render nahi hua. Green/yellow = render hua (jittna yellow, utna time laga).

---

## Summary

✅ **Re-render Visualizer** — mini-project, teeno concepts ko ek saath  
✅ `RenderCount` component — ref se render count track karta hai  
✅ **React.memo** — Sidebar aur ExpensiveCard ko wrap kiya, unnecessary renders skip  
✅ **useCallback** — function reference stable kiya, memo kaam karne laga  
✅ **useMemo** — heavy calculation cache kiya, count change pe nahi chal raha  
✅ **Profiler** — flamegraph se verify kiya ki render skip ho raha hai

---

## Agla Step

Memo, useMemo, useCallback ho gaye. Ab baat karte hain complex state management ki — jab `useState` kaafi nahi hota, tab `useReducer` kaam aata hai. Woh 5.5 mein.
