Here's where it gets real. dev-B's links branch is behind main (main now has the hero commits). If we try to merge links without syncing, we might hit conflicts — and that's intentional.

**Before merging the links PR, sync it with main:**

On dev-B's machine:

```bash
git switch feature/links-section
git fetch origin
git merge origin/main
```

Watch what happens. Both branches touched `index.html` (hero section vs links section) and `style.css` (hero styles vs link card styles). Git might auto-merge without conflicts if the sections are far enough apart. Or it might conflict.

If you get a conflict:

```
Auto-merging index.html
CONFLICT (content): Merge conflict in index.html
Automatic merge failed; fix conflicts and then commit the result.
```

Open the file. You'll see markers. Both dev-A and dev-B added content to `index.html` — dev-A added the hero section, dev-B added the links section. Depending on where they were placed, Git might be confused about ordering.

**Resolve the conflict:**

Edit the file. Keep BOTH sections (hero first, then links). Delete the markers. Save.

```bash
git add index.html
# also check style.css for conflicts
git commit
```

The merge commit now has both hero AND links in your branch.

```bash
git push origin feature/links-section
```

Now the PR is up to date. dev-A reviews, approves, merges.

**What just happened:**

This is the scenario from 1.5 — syncing your branch with main to prevent a conflict bomb. Except here, you deliberately let it happen so you could practice resolving it.

**The git tree after both merges:**

```
$ git tree
*   9a8b7c6 (HEAD -> main) Merge pull request #2
|\
| * 2b3c4d5 Sync with main (merge commit from dev-B)
| |\
| | * f5e6d7c (origin/main) Add hero section
| * | 1a2b3c4 Add links section
| |/
|/
* 4d3e2f1 Initial project skeleton
```

Notice the shape — two branches diverged, then came back together. That's real team history.

**Why this was the point:**

You deliberately created a situation where two people work in parallel, one merges early, the other needs to sync. This is THE most common real-world scenario. And you just handled it.

---

**In Your Own Words:**

<details>
<summary>1. What command syncs dev-B's branch with the latest main?</summary>

`git fetch origin` then `git merge origin/main` (while on dev-B's feature branch).
</details>

<details>
<summary>2. Why might the sync create a merge conflict even though dev-A and dev-B edited different sections?</summary>

If the sections are close together in the file, or if Git can't determine the order, it asks a human to decide.
</details>

<details>
<summary>3. What should you do after resolving the conflict and committing the merge on your branch?</summary>

Push the branch (`git push origin feature/links-section`) so the PR updates on GitHub.
</details>
