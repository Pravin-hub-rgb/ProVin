Jab tum Google pe kuch search karte ho — "best next js course" — toh results mein kya dikhta hai? Har result mein ek **title** hota hai, ek **description** (chhoti si line), aur ek URL. Google ye sab **page ke metadata** se uthata hai — HTML ke `<head>` section mein `<title>` aur `<meta name="description">` tags.

React course mein ye sab manually karte the — `<title>` tag, `<meta>` tags, har page pe alag. Aur jaise hi data dynamic hua (blog post ka title), meta bhi dynamic karna padta tha. Next.js mein iska better solution hai — **Metadata API** aur `generateMetadata`. Chalo seekhte hain.

---

## Manual Approach — React Mein Manual `<head>` Tags

React mein metadata manually manage karte the:

```tsx
// React — manual head tags
import { useEffect } from 'react';

export default function BlogPost() {
  useEffect(() => {
    document.title = "My Blog Post";   // browser tab title
  }, []);

  return <h1>My Blog Post</h1>;
}
```

`document.title` set karte the — koi `<meta name="description">` nahi, koi Open Graph nahi. Aur jaise hi page dynamic hua, `useEffect` mein title change karna padta tha. **Manual, fragile, aur SEO ke liye kharab** — Google useEffect-se-set title ko thik se crawl nahi karta (kabhi JS ke bina page dekhta hai).

---

## Better Approach — Next.js Metadata API

Next.js mein metadata **server-side** set hota hai — HTML ke andar seedha, `useEffect` ki zaroorat nahi. Do tareeke hain:

### Static Metadata — `export const metadata`

File se metadata export karo — simple object:

```tsx
// app/about/page.tsx
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "About Me | Pravin",
  description: "Main ek full-stack developer hun — Next.js, React, TypeScript.",
};

export default function About() {
  return <h1>About Me</h1>;
}
```

Next.js ise page ke HTML ke `<head>` mein daal dega — browser tab mein title, search engine ko description. **Server-side, koi useEffect nahi.**

### Dynamic Metadata — `generateMetadata`

Ab asli power — metadata ko **dynamic** banana. Blog post ka title hi uski metadata banna chahiye.

**Pehle samjho — `generateMetadata` kya hai?**

Ye Next.js ka **reserved function** hai — tum isko naam nahi de sakte. Jaise `page.tsx` ka naam fixed hai (page component ke liye), waise hi `generateMetadata` ka naam fixed hai (metadata ke liye).

```tsx
// ❌ Galat — naam badal diya
export async function getPageMetadata({ params }) {
  return { title: "..." };
}

// ✅ Sahi — naam fixed hai
export async function generateMetadata({ params }) {
  return { title: "..." };
}
```

**Rules:**
| Rule | Detail |
|------|--------|
| Naam fixed | `generateMetadata` likhna padega, kuch aur nahi chalega |
| Export zaroori | `export function generateMetadata` hona chahiye |
| Server Component | Client Component mein nahi chalta |
| Async allowed | `async function generateMetadata` — fetch ke liye |
| Params milta hai | Dynamic routes ke liye `params` argument aata hai |
| Return type | `Metadata` type return karna hai |

**Ye React ka feature nahi hai** — ye sirf Next.js App Router mein hai. React mein tumhe khud `<head>` manage karna padta tha.

**Kaise kaam karta hai?**
1. User `/blog/pehla-post` pe aata hai
2. Next.js sabse pehle `generateMetadata({ params })` call karta hai
3. Jo metadata return hota hai, Next.js use HTML ke `<head>` mein daal deta hai
4. Phir page render hota hai

Ab code dekhte hain:

```tsx
// app/blog/[slug]/page.tsx
import type { Metadata } from "next";
import { getPostBySlug } from "@/lib/posts";

interface BlogPostProps {
  params: Promise<{ slug: string }>;
}

export async function generateMetadata({ params }: BlogPostProps): Promise<Metadata> {
  const { slug } = await params;
  const post = getPostBySlug(slug);

  if (!post) {
    return { title: "Post Not Found" };
  }

  return {
    title: post.title,
    description: post.excerpt,
  };
}

export default async function BlogPost({ params }: BlogPostProps) {
  const { slug } = await params;
  const post = getPostBySlug(slug);
  // ... render
}
```

**Plain language mein samjho:**

```
1. generateMetadata ko params milta hai → { slug: "pehla-post" }
2. Usse slug extract karo → "pehla-post"
3. Database se post lao → post.title = "Mera Pehla Blog Post"
4. Return karo → { title: "Mera Pehla Blog Post", description: "..." }
5. Next.js HTML mein daal deta hai → <title>Mera Pehla Blog Post</title>
```

**Dekho pattern:** `generateMetadata` ko `params` milta hai (1.3 se yaad — Next.js call karta hai!), post fetch karta hai, aur title/description return karta hai. Har slug ka apna title — `/blog/pehla-post` pe "Mera Pehla Blog Post" title dikhega.

> **Important:** Page render hone se pehle `generateMetadata` chalata hai — Next.js isse SEO ke liye HTML mein daal deta hai.

---

## Metadata Template — Root Layout Mein Default

Sab pages ka title "site | page name" jaisa hona chahiye — har page pe repeat nahi karna. Root `layout.tsx` mein **template** define karo:

```tsx
// app/layout.tsx
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: {
    default: "Pravin | Full-Stack Developer",
    template: "%s | Pravin",
  },
  description: "Pravin ka portfolio — Next.js, React, TypeScript.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

**Template kaise kaam karta hai?**

`%s` ek **placeholder** hai — iski jagah jo page title set hota hai wo aa jaata hai.

```tsx
template: "%s | Pravin"
//      ^^^ page ka title yahan aata hai
```

**Example 1: About Page**
```tsx
// app/about/page.tsx
export const metadata = {
  title: "About",  // %s ki jagah "About" aa jayega
};
// Result: <title>About | Pravin</title>
```

**Example 2: Projects Page**
```tsx
// app/projects/page.tsx
export const metadata = {
  title: "Projects",  // %s ki jagah "Projects" aa jayega
};
// Result: <title>Projects | Pravin</title>
```

**Example 3: Blog Post (Dynamic)**
```tsx
// app/blog/[slug]/page.tsx
export async function generateMetadata({ params }) {
  const { slug } = await params;
  return {
    title: "My First Blog Post",  // %s ki jagah ye aa jayega
  };
}
// Result: <title>My First Blog Post | Pravin</title>
```

**Example 4: Page Jo Kuch Set Nahi Karta**
```tsx
// app/contact/page.tsx
// title set nahi kiya
export default function Contact() {
  return <h1>Contact</h1>;
}
// Result: <title>Pravin | Full-Stack Developer</title> (default use hoga)
```

**Summary Table:**

| Page | Title Set | Browser Tab Mein Dikhega |
|------|-----------|--------------------------|
| About | `title: "About"` | `About \| Pravin` |
| Projects | `title: "Projects"` | `Projects \| Pravin` |
| Blog Post | `title: "My First Blog Post"` | `My First Blog Post \| Pravin` |
| Contact | Title nahi set | `Pravin \| Full-Stack Developer` (default) |

**Ek jagah define, sab pages pe consistent.** Har page mein `" | Pravin"` likhne ki zaroorat nahi — template khud handle karta hai.

---

## Open Graph — Social Media Share

LinkedIn/WhatsApp pe link share karte ho toh ek preview dikhta hai — title, description, image. Wo **Open Graph** tags hain:

```tsx
export const metadata: Metadata = {
  title: "My Blog Post",
  description: "Post ka excerpt.",
  openGraph: {
    title: "My Blog Post",
    description: "Post ka excerpt.",
    images: ["https://example.com/blog-image.png"],
  },
};
```

`openGraph` object se social platforms ko preview data milta hai. Baad mein aage batches mein ise aur detail mein use karenge.

---

## Minimal Example — Product Page With Metadata

Ab 2.1 ke product page mein metadata jodte hain:

```tsx
// app/products/page.tsx
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Products",
  description: "Hamari products ki list — latest tech gadgets.",
};

interface Product {
  id: number;
  title: string;
  price: number;
}

interface ProductsResponse {
  products: Product[];
}

export default async function ProductsPage() {
  const res = await fetch("https://dummyjson.com/products", {
    next: { revalidate: 60 },
  });
  const data: ProductsResponse = await res.json();

  return (
    <main>
      <h1>Products</h1>
      <ul>
        {data.products.map((product) => (
          <li key={product.id}>
            {product.title} — ${product.price}
          </li>
        ))}
      </ul>
    </main>
  );
}
```

Ab browser tab mein "Products | Pravin" dikhega (layout template se), aur description SEO ke liye HTML mein hai.

---

## Verify Karna — DevTools Mein

Metadata set karne ke baad verify karo:

1. Browser mein `/products` kholo
2. Right-click → **Inspect** → `<head>` section kholo
3. `<title>Products | Pravin</title>` aur `<meta name="description" content="...">` dikhna chahiye

Next.js ne HTML mein ye tags **server-side** daale hain — JavaScript disabled hone par bhi dikhenge (React ke `useEffect` se opposite — wo JS ke bina nahi dikhta).

---

## Nutshell

Metadata SEO ka base hai — title + description. Next.js mein `export const metadata` (static) ya `generateMetadata` (dynamic) se set hota hai, server-side. Root layout mein `template` se consistent titles. Open Graph se social preview. React ke manual `<title>` tags se better kyunki server-side render hota hai — Google usko properly crawl karta hai.

---

## Common Mistakes

- ❌ `generateMetadata` mein page ka data dobara fetch karna — page aur metadata dono alag fetch karte hain to waste. Same data ko shared function se lao ya `cache()` use karo (aage batch mein).
- ❌ Metadata mein `any` type — `Metadata` type import karo (`import type { Metadata } from "next"`), saari fields typed hain.
- ❌ `generateMetadata` ko async banana bhoolna jab fetch ho raha ho — `await params` aur `await fetch` ke liye async zaroori hai.
- ❌ Root layout mein `title` string ke roop mein likhna jab template use kar rahe ho — `{ default, template }` object use karo, warna template override ho jayega.
- ❌ Description na dena — title important hai, par description Google result mein chhota text hai, wo bhi zaroori hai.

---

## In Your Own Words

1. React mein metadata kaise set karte the, aur Next.js mein kya fark hai?

<details>
<summary>Sample Jawab</summary>

**Jawab:** React mein `document.title` ko `useEffect` se set karte the — manual, fragile, aur Google ko proper SEO value nahi milti (JS ke bina page mein title nahi hota). Next.js mein `export const metadata` object se set karte hain — server-side HTML mein aata hai, Google properly crawl karta hai.

</details>

2. `generateMetadata` ka kya fayda hai? Static `metadata` se kab zyada useful?

<details>
<summary>Sample Jawab</summary>

**Jawab:** `generateMetadata` metadata ko dynamic banata hai — `params` se data fetch karke title/description return karta hai. Jab metadata page ke data pe depend kare (blog post ka title, product ka naam), tab static object nahi chalega — `generateMetadata` hi sahi hai.

</details>

3. Root layout mein `title: { default, template }` ka matlab kya hai?

<details>
<summary>Sample Jawab</summary>

**Jawab:** `template: "%s | Pravin"` — jab kisi page mein `title: "Projects"` set karega, template se "Projects | Pravin" banta hai. `default` wo title hai jab koi page kuch set na kare. Ek jagah define, consistent titles sab pages pe.

</details>

4. `generateMetadata` ko `params` kaise milta hai — usse koi call karta hai kya?

<details>
<summary>Sample Jawab</summary>

**Jawab:** 1.3 wala pattern hi hai — Next.js call karta hai. User `/blog/pehla-post` pe aata hai, Next.js `generateMetadata({ params })` ko khud call karta hai `params.slug = "pehla-post"` de kar. Humne signature declare kiya, Next.js ne provide kiya.

</details>

5. `generateMetadata` ka naam badal sakte hain kya? `getPageMetadata` ya kuch aur?

<details>
<summary>Sample Jawab</summary>

**Jawab:** Nahi. `generateMetadata` Next.js ka reserved function hai — naam fixed hai. Tum ise `getPageMetadata` ya kuch aur nahi bana sakte. Jaise `page.tsx` ka naam fixed hai (page component ke liye), waise hi `generateMetadata` ka naam fixed hai (metadata ke liye). Next.js ko pata hai ki ye function kahan se call karna hai — naam badlega toh kaam nahi karega.

</details>

---

## What It Is NOT

⚠️ **Metadata = "sirf browser tab title" nahi.** Title, description, Open Graph, Twitter cards — sab SEO + social sharing ke liye. Google, LinkedIn, WhatsApp sab yahi HTML tags padhte hain.

⚠️ **Metadata = "visibility" guarantee nahi.** SEO metadata set karne se page Google mein turant nahi aa jata. Metadata **rank hone ke liye zaroori condition** hai, par guarantee nahi.

⚠️ **`generateMetadata` = "render-time fetch" nahi.** Ye build time (static) ya request time (dynamic) pe chal sakta hai — rendering strategy (2.3) se decide hota hai. `generateMetadata` khud strategy nahi badalta.

---

Batch 2 ke saare concepts ready hain — server fetch (2.1), client components (2.2), rendering + revalidate (2.3), metadata (2.4). Ab time hai inko **ek jagah jodne ka** — Batch 1 ke Portfolio/Blog Site ko v2 mein upgrade karenge. Agla: **Combined Project — Portfolio/Blog Site v2.**
