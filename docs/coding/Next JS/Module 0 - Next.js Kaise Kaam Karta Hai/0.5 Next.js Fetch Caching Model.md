0.4 tak hum sunn gaye — server ready HTML bhejta hai, client interactivity jodta hai. Par **server WOH data kahan se laata hai?** Database jaisi jagah se toh har baar thodi der lagti hai. Next.js isko smartly handle karta hai ek pine — **fetch caching model**.

Socho tum milkshake banana seekher rahe ho. Recipe tumhare paas hai. Agar **bar-bar recipe website download** karte raho, toh time lagta hai. Par agar ek baar recipe read karli, use **recipe list pe likh lo**, toh agle baar turant milti hai.

## "Kahin Ho To Wahan Se, Nahin To Banao" — Caching Ka Base

**Cache** = ek storage jahan koi cheez pehle se store hoti hai, taaki dobara compute karna na pade.

**Fetch caching** ka matlab: Server Component jis API/DB se data laata hai, uska result ek jagah store kar do. Agli baar wahi request aaye toh **server se dobara nahi jana** — cached copy turant milti hai.

> **Key:** Caching ka fayda = **speed** (server/data-source pe load kam, response fast). Lekin downside = **data stale** ho sakta hai (jab tak invalidate na karein).

## Next.js Default Fetch Behavior (App Router)

`fetch()` Server Component mein use hota hai toh Next.js default mein **cache** karta hai — data **build time pe** fetch + cached hota hai (static). Yeh SSG (0.2) ko enable karta hai — pehle se ready.

```tsx
// app/page.tsx — DEFAULT
export default async function Page() {
  const res = await fetch("https://api.example.com/posts");
  const posts = await res.json();
  return <PostList posts={posts} />;
}
```

By default (App Router, Next 15+) `fetch` **cached** hai. Matlab pehli baar run pe data API se aaya aur store hua — baad ken requests cached copy use karenge.

### But You Can Tell It When NOT To Cache

Agar data har baar fresh chahiye (live stock, real-time) — `cache: 'no-store'`:

```tsx
const res = await fetch("https://api.example.com/prices", {
  cache: "no-store",   // har request pe fresh — server se
});
```

Agar thodi der ke liye revalidate karna hai (e.g. 60 seconds pe refresh) — `next: { revalidate: 60 }`:

```tsx
const res = await fetch("https://api.example.com/posts", {
  next: { revalidate: 60 },  // 60 sec baad dobara fetch
});
```

(Full `revalidate` + ISR mechanism 0.6 mein clearly samjhenge — yahan bas formats dikha rahe hain.)

## Data Fetching Options — Quick Table

| Karne Ka Tarika | Kya Hota Hai | Kab Use |
|---|---|---|
| Default `fetch()` | Cached (static), build time pe | Rarely-changing public data (blogs, catalog) |
| `cache: 'no-store'` | Har request pe fresh | Live data (prices, notifications, user-specific) |
| `next: { revalidate: 60 }` | 60s tak cached, phir refresh | Data jo thoda der mein refresh hota (ISR — 0.6) |

## Drinks Helpline — Kya Cache Karo Kya Nahi

- **Kya cache karo:** Jo data **rarely badta hai** (retail product info, help docs, blog posts) — SSG fayda
- **Kya cache MAT karo:** Jo har visitor ke liye **alag hai** (user dashboard, cart, personalized feed) — `no-store` → fresh

> Hamesha yeh decide karte time — cache karne se performance badhta hai par stale data; `no-store` se fresh par server load.

## What It Is NOT

⚠️ **Caching = har jagah static HTML nahi.** Request ko data ke liye har baar source jane ki jagah store copy serve karne wala pattern hai — static hona static-asset-hi, dynamic bhi ho sakta hai.

⚠️ **Cache = browser cache ke same nahi bol sakte.** Browser cache client pe hota hai; yeh Next.js server-side fetch cache hai — different liye (server reduce backend load).

⚠️ **`no-store` = invisible toh nahi matlab bad.** Iska matlab hi single source pe jana — ek dynamic endpoint. ISR bhi `revalidate` se use ho jata hai (0.6).

## Summary Humne Jani

- **Cache** = dobara compute na karke store copy use karna (speed + low load)
- Next.js App Router `fetch()` **default cached** hai (static/SSG)
- `cache: 'no-store'` → har har request pe **fresh**
- `next: { revalidate: 60 }` → thodi der ke liye cached, phir refresh
- Decide: rarely-changing → cache; per-user/live → no-store

---

**In Your Own Words**

1. Caching ka fayda aur downside ek-line mein kya hai?

<details>
<summary>Sample Jawab</summary>

**Jawab:** Fayda — speed aur backend/data-source pe load kam (dobara compute nahi). Downside — data stale/saaya ho sakta hai (jab tak invalidate/invalidate na kare).

</details>

2. App Router mein default `fetch()` cached hai ya nahi — aur kya hota hai?

<details>
<summary>Sample Jawab</summary>

**Jawab:** Haan, default cached hai (static/SSG) — pehli baar fetch ho kar store, baad ke requests cached copy use karte hain. Matlab build time pe data ready.

</details>

3. `cache: 'no-store'` kab use karte hain aur kya karta hai?

<details>
<summary>Sample Jawab</summary>

**Jawab:** Jab har request pe fresh data chahiye (live prices, user-specific dashboard). Ye cache nahi karta — har request pe source (server/API) se fresh data lata hai.

</details>

4. Kya cache karna chahiye aur kya nahi — ek simple rule?

<details>
<summary>Sample Jawab</summary>

**Jawab:** Jo data rarely badta hai aur public hai (blog, catalog) — cache. Jo har user ke liye alag hai ya live hona chahiye (dashboard, cart, live prices) — no-store. Decide speed vs freshness tradeoff.

</details>

---

Humne dekha `next: { revalidate: 60 }` se cache ko thodi der baad refresh kar sakte hain. Par iska full power toh usme hai jab hume **page pe** lagana hai — static page jo time-to-time khud refresh ho jaye. Yehi hai **Incremental Static Regeneration (ISR)** — 0.6 mein detail se.