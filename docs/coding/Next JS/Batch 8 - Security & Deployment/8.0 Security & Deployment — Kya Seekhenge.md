Batch 7 ke end mein Photo Gallery ban gaya tha — optimized images (`next/image`), self-hosted font (`next/font`), streaming grid (Suspense), Instagram-style modal (parallel + intercepting). App production-feel deta hai — fast, smooth, user-friendly. Par ab ek naya sawaal uthta hai — **kya app safe hai aur kya deploy ho sakta hai?** Database sahi hai, images optimize hain, UI fast hai — par agar `DATABASE_URL` galti se client bundle mein expose ho jaaye, ya `console.log` mein secret print ho jaaye, ya koi form bina validation ke data accept kar le — toh koi bhi hacker access le sakta hai. Aur agar app sirf local machine pe chalta hai toh kya fayda? **Production pe aana chahiye.**

Batch 6 ne data ka foundation diya (Prisma + Neon), Batch 7 ne user experience optimize kiya (images, fonts, streaming). Ab Batch 8 mein in dono ko **safe + live** banayenge — security audit, error handling at scale, aur Vercel pe deploy. **Yeh course ka final polish batch hai** — iske baad capstone ke liye ready ho.

---

## Kya Seekhenge?

| # | Concept | Kab kaam aayega |
|---|---------|-----------------|
| 8.1 | Env Vars — Server-Only vs `NEXT_PUBLIC_` | Jab secret (DATABASE_URL, API keys) safe rakhna ho — kya server pe dikhana hai, kya client pe |
| 8.2 | Input + CSRF (Awareness) | Jab form ya mutation ho — client input pe trust mat karo, Server Actions ka built-in protection samjho |
| 8.3 | Error Boundaries at Scale | Jab production app mein errors gracefully handle ho — route-level se le ke layout + global-error tak |
| 8.4 | Deployment (Vercel) | Jab app ko live karna ho — git push se Vercel pe deploy, env vars, production build |
| 8.5.x | Project — Security Audit + Deploy | Photo Gallery (Batch 7) ka audit + live deploy |

---

## Batch Ka Spine — "Manual → Better"

Roadmap ka core principle yahan bhi continue — **pehle manual tareeke ki problem feel karo, phir Next.js ka better solution:**

| Topic | Manual (pehle problem feel) | Better (Next.js/Production solution) |
|---|---|---|
| Secrets | Hardcoded secret / `NEXT_PUBLIC_` misuse → client bundle mein expose | `.env` server-only vars, `NEXT_PUBLIC_` prefix rules, `.gitignore` |
| Input/CSRF | Client input pe trust / XSS / CSRF attack | Server Actions built-in CSRF (Origin check) + server-side validation |
| Errors | Raw errors, no boundary, layout crash = white screen | `error.tsx` at scale + `global-error.tsx` hierarchy |
| Deploy | Self-host (build/start, env management pain) | Vercel — git push, zero-config, env per environment |

Har topic mein pehle **problem feel karoge** (manual), phir **solution** (production-grade). Yahi "why" clear karta hai, sirf "how" nahi.

---

## Is Batch Mein Kya NAHI Hoga

Scope clear rakhna zaroori hai:

- **Naya project nahi** — Batch 7 ka Photo Gallery hi audit + deploy hoga. Roadmap kehta hai "koi naya nahi — security audit + deploy."
- **Manual CSRF token implementation nahi** — awareness level (Server Actions ka built-in protection samjho, token khud mat banao).
- **Security headers ka full course nahi** — Batch 4 (proxy.ts) mein already cover hai. Yahan sirf mention karenge.
- **JIT/edge deployment nahi** — Vercel pe standard Node.js deploy.
- **Koi auth add nahi** — Photo Gallery read-only hai, auth Batch 5 mein already covered hai.

---

## Combined Project — Photo Gallery: Security Audit + Deploy

Roadmap kehta hai: *"Poore project ka security audit — kya galti se client-expose ho raha hai — aur poora project Vercel pe deploy."*

Toh **koi naya project nahi banega.** Batch 7 ka Photo Gallery abhi local machine pe chal raha hai — usse:
1. **Security audit** karenge (env vars, NEXT_PUBLIC misuse, error boundaries, build)
2. **Global-error.tsx** add karenge (batch ka error handling at scale)
3. **Vercel pe deploy** karenge (live URL milegi)

4-part series mein hoga (planning → env audit → error boundaries → deploy), wahi "how to write project in series" process. **Naya code nahi — existing code ko safe + live banana.**

---

## Prerequisite

Ye batch shuru karne se pehle ye hona chahiye:

- ✅ Batch 7 complete — Photo Gallery built + running (images, fonts, streaming, modal)
- ✅ Batch 6 — Prisma + Neon (DATABASE_URL `.env` mein hai — isse audit karenge)
- ✅ Batch 5 — Auth basics (awareness — CSRF ka context samajh aayega)
- ✅ Batch 4 — proxy.ts (security headers ka reference)
- ✅ Batch 1.4 — error.tsx basics (error, reset — ab at scale karenge)

---

## Nutshell

Batch 8 mein **security + deployment** seekhenge — Photo Gallery ko safe aur live banayenge. Chaar "Manual → Better" pairs: **secrets** (hardcoded → `.env` server-only), **input/CSRF** (trust → Server Actions built-in protection), **errors** (raw → `error.tsx` + `global-error.tsx`), aur **deploy** (self-host → Vercel). End mein Photo Gallery ka security audit + Vercel deploy — **koi naya project nahi, existing code ko production-ready banana.** Yeh course ka final polish hai — iske baad capstone ke liye ready.

Pehla step: env vars kyun secure honi chahiye — hardcoded secret ki asli problem. 8.1 mein shuru karte hain.
