8.5.2 mein env + config audit ho gaya — safe hai. Ab **error boundaries** add karte hain. Photo Gallery mein abhi sirf basic `error.tsx` hai (1.4 level). Production app mein chahiye:

1. **`global-error.tsx`** — root layout crash ka last resort
2. **Route-level error boundaries** — har route pe proper error handling
3. **Verify hierarchy** — error boundary hierarchy sahi kaam kar rahi hai

---

## Step 1 — `global-error.tsx` Add Karo

Sabse pehle **root `app/` folder** mein `global-error.tsx` banao:

**File:** `app/global-error.tsx` — naya file

```tsx
'use client'

export default function GlobalError({
  error,
  reset,
}: {
  error: Error & { digest?: string }
  reset: () => void
}) {
  return (
    <html lang="en">
      <body>
        <div style={{
          display: "flex",
          flexDirection: "column",
          alignItems: "center",
          justifyContent: "center",
          minHeight: "100vh",
          fontFamily: "system-ui, sans-serif",
        }}>
          <h2 style={{ fontSize: "1.5rem", marginBottom: "1rem" }}>
            Kuch bahut bura ho gaya
          </h2>
          <p style={{ color: "#666", marginBottom: "1.5rem" }}>
            {error.message || "Unexpected error occurred"}
          </p>
          <button
            onClick={() => reset()}
            style={{
              padding: "0.75rem 1.5rem",
              backgroundColor: "#0070f3",
              color: "white",
              border: "none",
              borderRadius: "8px",
              cursor: "pointer",
              fontSize: "1rem",
            }}
          >
            Try Again
          </button>
        </div>
      </body>
    </html>
  )
}
```

**Important:**
- `'use client'` zaroori hai
- `<html>` aur `<body>` likho — root layout replace hota hai
- Clean, friendly UI — user ko dikhna chahiye

---

## Step 2 — Photo Detail Route Error Boundary

Photo Gallery mein `/photo/[id]` route hai — wahan bhi error boundary add karo:

**File:** `app/photo/[id]/error.tsx` — naya file

```tsx
'use client'

export default function PhotoError({
  error,
  reset,
}: {
  error: Error & { digest?: string }
  reset: () => void
}) {
  return (
    <div style={{
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      padding: "4rem 2rem",
    }}>
      <h2 style={{ fontSize: "1.25rem", marginBottom: "1rem" }}>
        Photo load nahi ho paayi
      </h2>
      <p style={{ color: "#666", marginBottom: "1.5rem" }}>
        {error.message || "Something went wrong loading this photo."}
      </p>
      <button
        onClick={() => reset()}
        style={{
          padding: "0.5rem 1rem",
          backgroundColor: "#0070f3",
          color: "white",
          border: "none",
          borderRadius: "6px",
          cursor: "pointer",
        }}
      >
        Dobara try karo
      </button>
    </div>
  )
}
```

---

## Step 3 — Error Hierarchy Verify

Ab Photo Gallery ki error hierarchy check karo:

```
app/
├── global-error.tsx         ← ✅ Added (root layout crash)
├── layout.tsx               ← Root layout
├── page.tsx                 ← / (home)
├── photo/
│   ├── [id]/
│   │   ├── error.tsx        ← ✅ Added (/photo/[id] errors)
│   │   └── page.tsx         ← /photo/:id
│   └── page.tsx             ← /photo (gallery list)
├── @modal/
│   └── (.)photo/[id]/
│       └── page.tsx         ← Modal (client component)
```

**Hierarchy flow:**
1. `/photo/5` mein error → `photo/[id]/error.tsx` catch karega ✅
2. Root layout mein error → `global-error.tsx` catch karega ✅
3. Gallery list (`/photo`) mein error → `app/error.tsx` (agar ho) ya `global-error.tsx` ✅

---

## Step 4 — Test Error Boundaries

1. `app/photo/[id]/page.tsx` mein temporarily throw daalo:
   ```tsx
   export default async function PhotoPage() {
     throw new Error("Test error!");
     // ... existing code
   }
   ```

2. `/photo/1` pe jaao → `photo/[id]/error.tsx` dikhega ✅
3. "Dobara try karo" click karo → page dobara render hoga ✅
4. Throw hata do — normal page wapas aa jaayega ✅

5. Ab `app/layout.tsx` mein throw karo → `global-error.tsx` dikhega ✅
6. Throw hata do ✅

---

## Step 5 — Build Verify

```bash
npm run build
```

**Expected:** Build successful, koi error naye files ki wajah se nahi aana chahiye.

---

## Audit Summary

| Check | Status | Notes |
|---|---|---|
| `global-error.tsx` | ✅ Added | Root layout crash - last resort |
| `photo/[id]/error.tsx` | ✅ Added | Route-level error handling |
| Error hierarchy | ✅ Verified | Route → layout → global |
| Build | ✅ | `npm run build` successful |

---

## Nutshell

Photo Gallery mein error boundaries add kiye: `global-error.tsx` (root crash - last resort) + `photo/[id]/error.tsx` (route-level). Hierarchy verify ki — route → layout → global. Error boundary audit complete. Agla step: deploy.

---

## Bridge

Error boundaries ready, env config safe. Ab **deploy** karna hai — Vercel pe live. 8.5.4 mein git push + Vercel import + env config + live verify.
