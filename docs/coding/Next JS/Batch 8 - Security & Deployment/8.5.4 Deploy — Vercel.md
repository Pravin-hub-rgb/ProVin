8.5.2 mein env + config audit ho gaya, 8.5.3 mein error boundaries add ho gaye. Photo Gallery ab **safe** hai. Ab **live** karte hain — Vercel pe deploy. Yeh batch ka last step hai — iske baad Photo Gallery ke paas **live URL** hogi.

---

## Step 1 — Final Build Check

Deploy se pehle ek baar **local production build** verify karo:

```bash
npm run build
```

**Expected:**
```
✓ Linting and checking validity of types
✓ Creating an optimized production build
✓ Compiled successfully
```

Build successful? ✅ Aage badho. Agar error aaye — pehle fix karo.

---

## Step 2 — Git Push

Saara code GitHub pe push karo:

```bash
git add .
git commit -m "Security audit + error boundaries + ready for deploy"
git push origin main
```

**Confirm:** `.env.local` git mein track nahi ho rahi (`.gitignore` mein listed hai). Agar ho rahi hai toh pehle `git rm --cached .env.local` karo.

---

## Step 3 — Vercel Mein Import

1. [vercel.com](https://vercel.com) → Sign in with GitHub
2. **"Add New..."** → **"Project"**
3. Apna Photo Gallery repo select karo
4. Vercel automatically detect karega:
   - **Framework:** Next.js ✅
   - **Build Command:** `next build` (default) ✅
   - **Output Directory:** `.next` (default) ✅
5. **"Deploy"** button dabao

Build shuru ho jaayegi. 1-2 minute mein deploy ho jaayega.

---

## Step 4 — Environment Variables Set Karo

Vercel dashboard → **Settings** → **Environment Variables**:

| Key | Value | Environment |
|---|---|---|
| `DATABASE_URL` | `postgresql://user:password@neon.tech/mydb` | Production, Preview |

**Important:**
- `.env.local` ki value wahi daalo
- **"Production"** aur **"Preview"** dono select karo — taaki preview deployments bhi kaam karein
- **"Development"** mat chuno — local ke liye `.env.local` hi use hota hai

---

## Step 5 — Redeploy (Env Vars Ke Baad)

Environment variables set karne ke baad **redeploy** karo:
- Vercel dashboard → **Deployments** tab → latest deployment pe click → **"..."** menu → **"Redeploy"**

Kyun? Pehle deploy mein env vars nahi the — isliye `DATABASE_URL` undefined tha. Redeploy se naya build env vars ke saath banega.

---

## Step 6 — Live Verify

Vercel ka live URL kholo (`.vercel.app` domain).

**Checklist:**

| Check | Kya dekhna hai |
|---|---|
| Home page | Gallery grid dikh rahi hai? |
| Photo detail | `/photo/1` pe image + details load ho rahe hain? |
| Images | `next/image` optimized load ho rahi hain? (WebP format, chhoti files) |
| Font | Inter font sahi hai? (koi FOUC nahi) |
| Streaming | Grid progressive load ho rahi hai? (skeleton → content) |
| Modal | Grid se photo click → modal khulta hai? |
| Direct URL | `/photo/1` direct visit pe full page dikhta hai? |
| Error | `/photo/999` (non-existent) pe error boundary dikhta hai? |
| 404 | `/nonexistent` pe custom 404 dikhta hai? |
| Security | DevTools → Sources → koi `DATABASE_URL` toh nahi dikh rahi? |

---

## Step 7 — Post-Deploy Security Check

Live site pe **DevTools → Sources tab** (Chrome) ya **Debugger tab** (Firefox):

1. Search karo `DATABASE_URL` ki value
2. Search karo `AUTH_SECRET` (agar hai)
3. Search karo `NEXT_PUBLIC_`

**Expected:** Kuch nahi milna chahiye. Agar mil jaaye — secret client bundle mein hai. Security issue.

---

## Batch 8 Complete — Summary

```
8.0  — Intro: Manual → Better pairs, koi naya project nahi
8.1  — Env vars: .env, server-only vs NEXT_PUBLIC_, .gitignore
8.2  — Input/CSRF: Server Actions built-in protection, server-side validation
8.3  — Error boundaries at scale: global-error.tsx, hierarchy, reset
8.4  — Deployment: Vercel, git push = deploy, env vars per environment
8.5.1 — Planning: Audit checklist + deploy plan
8.5.2 — Audit: Env vars, .gitignore, remotePatterns, build ✅
8.5.3 — Audit: Error boundaries added (global-error + route-level) ✅
8.5.4 — Deploy: Vercel live ✅
```

**Photo Gallery ab live hai** — optimized images, self-hosted font, streaming, modal, **security-audited**, **deployed on Vercel.** Yeh Batch 8 ka result hai — "Manual → Better" pattern ab tumhare paas hai: hardcoded secrets → `.env` server-only, trust client → Server Actions built-in CSRF, no error handling → `error.tsx` hierarchy, self-host → Vercel.

**Batch 8 complete.** Data (Batch 6) + Optimization (Batch 7) + Security & Deployment (Batch 8) — ab **capstone** ke liye ready ho. Capstone mein production-grade project banega — sab kuch jo seekha hai woh combine hoga.
