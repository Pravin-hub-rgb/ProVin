6.10 mein setup complete hua — `drizzle_todos` table Neon mein ban gayi, `db` client taiyaar hai. Ab asli Todo app banate hain — par **is baar pattern alag hai**. 6.7.1 se 6.7.4 mein Mongo Todo API + UI REST route handlers se banayi thi (frontend `fetch` karta tha — useEffect + client components). **Yahan SQL Todo Server Components + Server Actions se banayenge** — reads SC mein seedha, mutations SA se. Roadmap ka R/S pattern yaad hai — ab wahi haath lagata hai.

Aur ek fark batata hoon jo poori phase ko shape deta hai: **Server Actions ko simple URL nahi hota** (Postman unhe test nahi kar sakta), isliye REST + API design + Postman Mongo phase mein the (6.6-6.8). SQL phase mein SC+SA pattern — direct server code, revalidatePath se UI refresh. Yeh series 4 parts mein hai — pehla (yeh file) **Read**, phir 6.11.2 mein Create, 6.11.3 mein Toggle+Delete, aur 6.11.4 mein dono phases ka full compare. Har part apna ek clear teaching point — 6.7.1-6.7.4 jaisa hi structure.

# SQL Todo — Part 1: Read (Server Component)

## Step 1 — Pehli Problem: Table Empty Hai

`drizzle_todos` bilkul khali hai — koi data nahi. Pehle ek dummy todo daalte hain (neon ke SQL Editor se), taaki fetch test karne par pata chale ki data aa raha hai:

```sql
INSERT INTO drizzle_todos (title, done) VALUES ('Pehla Drizzle Todo', false);
```

Verify:

```sql
SELECT * FROM drizzle_todos;
```

```
id | title               | done
---+---------------------+------
1  | Pehla Drizzle Todo  | false
```

Data aa gaya — `id` automatically 1 (SERIAL). Ab aage.

## Step 2 — Read: Server Component Mein Fetch Karo

**SOCH:** Todo app ka home page = list dikhana. **Mongo phase mein** `/api/todos` route bana tha, frontend `fetch()` karta tha, client component state mein data rakhta tha. **Yahan kya alag?** Server Component hi server pe data fetch kar sakta hai — **seedha database se** — koi API layer nahi chahiye. `app/page.tsx` ko `async` bana do, andar `db.select()`, aur data directly render.

**CONCEPT:** Server Component (Batch 1 mein padha tha) = server pe render hota hai. Database bhi server pe hai. Toh page hi database se seedha query chalata hai — ek request, koi client round-trip nahi.

Pehla sawaal: **data UI mein aane ke liye `/api/todos` jaise URL kyun nahi chahiye?** Kyunki fetch ek client-side problem tha — client alag machine pe hai, isliye server tak pahunchne ke liye network call karni padti thi. Yahan jo page render hota hai woh server pe hi hai, database bhi server pe hi hai — dono ek hi jagah. Network ka rasta hai hi nahi, isliye page seedha query chala sakta hai. `app/page.tsx` ko `async` bana do, andar `db.select()`, aur data directly render.

**File:** `app/page.tsx` — ab file line-by-line banate hain:

```tsx
// app/page.tsx
import { db } from "@/db";
```

**`db` import** — 6.10 wala Drizzle client, ready export. Mongo phase mein `connectDB()` await karna padta tha connection ke liye; yahan client serverless ke liye already ready hai — connection ke intezar ki zaroorat nahi.

```tsx
import { todos } from "@/db/schema";
```

**`todos` import** — 6.10 ka schema export, TypeScript variable. Database table ka naam `drizzle_todos` hai, TypeScript mein `todos` se access hota hai — ek hi table, do names (neeche phir confirm karenge).

```tsx
import { desc } from "drizzle-orm";
```

**`desc` import** — ordering helper, `drizzle-orm` library se (wahi library jisne 6.10 mein `db` banaya).

```tsx
export default async function Home() {
```

**`async` Server Component** — page async ho sakta hai kyunki server pe render hota hai, aur `await db` ke liye yeh zaroori hai. Mongo phase ke `connectDB` wale await se compare karo — wahan connection ke liye await karna padta tha, yahan `db` seedha ready hai, await sirf query ke liye.

```tsx
  const allTodos = await db.select().from(todos).orderBy(desc(todos.id));
```

**`db.select().from(todos)`** — SQL `SELECT * FROM drizzle_todos`. `.orderBy(desc(todos.id))` — naya todo upar (descending). Yeh Drizzle khud SQL banata hai — tum raw SQL nahi likhte (6.10 mein compare tha).

```tsx
  return (
    <main>
      <h1>SQL Todo App</h1>
      <ul>
        {allTodos.map((todo) => (
```

**`allTodos.map((todo) => ...)`** — server pe array render — koi client state nahi, koi fetch result nahi. `allTodos` seedha database se aaya, ab seedha JSX mein:

```tsx
          <li key={todo.id}>
            {todo.title} {todo.done ? "(done)" : ""}
          </li>
```

**`key={todo.id}`** — id number hai (Mongo mein `_id` string tha — 6.7.x wale JSON mein dekha). `todo.title` / `todo.done` — schema ke fields, type safety Drizzle se.

```tsx
        ))}
      </ul>
    </main>
  );
}
```

**`main` close** — poori file taiyaar: import → async page → query → render.

**Important — name check:** 6.10 mein table export `todos` tha (import `todos` from `@/db/schema`). `drizzle_todos` database table ka naam hai, TypeScript mein variable `todos` se access karte hain. Confuse mat hona — ek hi table, do names (DB name + TS variable).

**TEST:** `npm run dev`, browser mein page kholo — "Pehla Drizzle Todo" dikhega. **Koi `/api/todos` URL nahi, koi fetch nahi, koi client state nahi** — direct server query. Yahi SC pattern ka fayda.

## Nutshell

Read operation SQL phase mein: `app/page.tsx` ko `async` banao, `db.select().from(todos).orderBy(desc(todos.id))` se database seedha query, result JSX mein render. Koi API route, koi fetch, koi client state nahi — Server Component server pe hi hai, database bhi server pe, isliye seedha baat.

## What It Is NOT

- **Server Component = "async render" nahi.** Server Component data fetch kar sakta hai kyunki woh server pe hi hai — database bhi server pe hai — isliye seedha query ho paati hai, koi network round-trip nahi. Yeh architectural choice hai, sirf syntax nahi.

---

**In Your Own Words**

1. SQL phase mein page ko `/api/todos` jaisa route kyun nahi chahiye tha, jabki Mongo phase mein chahiye tha?

<details>
<summary>Show Answer</summary>

**Sample Answer:** Mongo phase mein client (browser) alag machine pe tha — server tak pahunchne ke liye URL + fetch chahiye tha. SQL phase mein page hi Server Component hai — server pe render hota hai, database bhi server pe hai — dono ek hi jagah. Network ka rasta hai hi nahi, isliye page seedha `db.select()` chala sakta hai.

</details>

2. `app/page.tsx` ko `async` kyun banana padta hai?

<details>
<summary>Show Answer</summary>

**Sample Answer:** Kyunki page ke andar `await db.select()` chalta hai — query ka result aane tak wait karna hota hai. `async` function hi `await` kar sakta hai. Server pe render hote waqt query chalti hai, result aate hi page complete render ho jata hai.

</details>

3. `db.select().from(todos)` kya karta hai, aur `.orderBy(desc(todos.id))` se kya badalta hai?

<details>
<summary>Show Answer</summary>

**Sample Answer:** `db.select().from(todos)` SQL `SELECT * FROM drizzle_todos` hai — table ki saari rows. `.orderBy(desc(todos.id))` unhe id ke descending order mein laata hai — naya todo upar. Drizzle yeh SQL khud banata hai — raw SQL nahi likhni padti.

</details>

4. `todos` aur `drizzle_todos` — dono ek hi table hain to confuse kyun hote hain?

<details>
<summary>Show Answer</summary>

**Sample Answer:** `drizzle_todos` database mein table ka actual naam hai. TypeScript mein wahi table `todos` variable se access hoti hai (schema export, 6.10). Ek hi table, do names — ek database wala, ek code wala.

</details>

---

Read ready hai — data database se seedha page pe. Ab agli problem: **user naya todo kaise banayega?** 6.11.2 mein Server Action + form se Create banayenge — `createTodo` action, `db.insert()`, aur `revalidatePath` se UI refresh.
